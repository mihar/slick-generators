- form_for @<%= singular_name %>, :html => {:class => 'nice'} do |f|
  = f.error_messages

  <%- for attribute in attributes -%>
  %p
    = f.label :<%= attribute.name %>
    = f.<%= attribute.field_type %> :<%= attribute.name %>
  <% end -%>
  
  %p.submit
    = f.submit :Shrani